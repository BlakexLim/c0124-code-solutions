/* exported firstChars */
function firstChars(length: number, string: string): string {
  let stringChars = '';
  for (let i = length; i < string.length; i++) {
    stringChars = string;
  }
  return stringChars;
}
