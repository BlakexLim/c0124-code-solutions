'use strict';
/* exported firstChars */
function firstChars(length, string) {
  let stringChars = '';
  for (let i = length; i < string.length; i++) {
    stringChars = string;
  }
  return stringChars;
}
